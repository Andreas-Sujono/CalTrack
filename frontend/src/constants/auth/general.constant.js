const event = 'AUTH';

export const actionTypes = {
  SET: `${event}SET`,
  RESET: `${event}RESET`,
  RESET_SET: `${event}RESET_SET`,
};

export const errorMessage = {
  SERVER_ERROR: 'Cannot connect to the server',
};

export const successMessage = {};